package com.workflowplatform.schema.domain;

/**
 * Supported field types for dynamic form schemas.
 */
public enum FieldType {
    TEXT,
    NUMBER,
    DATE,
    BOOLEAN,
    ENUM,
    FILE_REF,
    USER_REF
}
